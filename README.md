# Тестовое задание от 21vek
Материалы: [задание](https://docs.google.com/document/d/1JU1CWaflt9Eyj1YOcaqYIwlAkXUODX6sK714dvLa4-w/edit?usp=sharing) и [датасет](https://drive.google.com/file/d/1dSTDrQ7RcOoGqwwabgyUW7jXNnUkRRyL/view?usp=sharing)

---

## ⚙️ Инструкция по запуску

### 1. Клонирование репозитория
```bash
git clone https://github.com/saiamaleykum/rec-sys.git
cd rec-sys
```

### 2. Настройка окружения
```bash
cp .env.example .env
```

### 3. Запуск через Docker
```bash
docker-compose up -d --build
```

### Конфигурация (.env)
| Переменная | Значение | Описание |
| :--- | :--- | :--- |
| CSV_DATA_PATH | `data/test.csv` | Путь к файлу с данными (относительно корня проекта) |
| W_CLICK | `1` | Вес клика (просмотра товара). |
| W_CART | `5` | Вес добавления в корзину. |
| W_PURCHASE | `10` | Вес покупки. |
| ITEMS_LIMIT | `5` | Количество товаров, которые вернет API в одном ответе. |
| BRAND_LIMIT | `2` | Максимальное количество товаров одного бренда в ответе. |

---

## 🧠 Алгоритм рекомендаций
### Формулы

1. Глобальный топ или **"популярность"**:
   $$Score_{global} = (U_{click} \cdot W_{click}) + (U_{cart} \cdot W_{cart}) + (U_{purchase} \cdot W_{purchase})$$

2. Персональный топ или **"интерес"**:
   $$Score_{user} = (N_{click} \cdot W_{click}) + (N_{cart} \cdot W_{cart})$$

**Условные обозначения:**
- $U$ — количество уникальных пользователей
- $N$ — общее количество действий пользователя
- $W$ — весовой коэффициент

### Возможные улучшения
* Учет фактора времени (свежие действия имеют больший вес, чем давние действия)
* Коллаборативная фильтрация (User-based или Item-based)
* Доверительный интервал Вилсона (если бы считали CTR)

---

## ❓ Ответы на вопросы
### Почемы вы выбрали именно такую структуру решения?
- Repository -> Service -> API
    + Разделение ответственности
    + Легко менять реализацию (например, заменить pandas на БД)
- Предрасчет общего ТОПа при старте
    + Не пересчитывается при каждом запросе
    + Быстрый ответ

### Какие у решения ограничения?
- Отсутствие кэширования
- Синхронные операции (из-за pandas)
- Производительность при старте (если csv ооочень большой, то будем ждать)
- Масштабируемость памяти (все в оперативке RAM)
- Простенький алгоритм рекомендаций
- Глобальный топ не обновляется (по-хорошему его лучше обновлять раз в N времени)
- Нет обработки случая, когда все товары куплены (вернет пустой список)
- Нет валидации настроек и входных данных (например, на отрицательность)
- Нет тестов, логирования, обработки ошибок и др.

### Что бы вы улучшили при росте числа пользователей?
- PostgreSQL + индексы + asyncpg 
- Кэширование (Redis по TTL, например, 5 мин для персональных рекомендаций и 60 мин для общих)
- Вынести синхронные операции (в потоки/процессы, celery, mq)

